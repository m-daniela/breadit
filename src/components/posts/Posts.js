import React from "react";
import PostsList from "./PostsList";

const Posts = () => {
    return (
        <div>
            Choose a post, try the recipe, comment etc.
            <PostsList />
        </div>
    );
};

export default Posts;
