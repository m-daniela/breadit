import React from "react";
import PostsList from "../posts/PostsList";

const Board = () => {
    return (
        <div className="board">
            <PostsList />
        </div>
    );
};

export default Board;
