import React from 'react';

/**
 * Dashboard page
 * This will be different based on the role 
 * (user, moderator, admin), but it will 
 * contain information about the posts and
 * comments the user has interacted with
 * TODO: this will be functional when user
 * TODO: support will be added
 */
const Dashboard = () => {
    return (
        <div>
            dashboard, different based on role
        </div>
    );
};

export default Dashboard;
