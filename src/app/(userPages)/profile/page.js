import React from "react";

/**
 * Profile page
 * The user will be able to see the profile 
 * information, as well as modify settings
 * TODO: this will be functional when user
 * TODO: support will be added
 */
const Profile = () => {
    return (
        <div>
            profile page, with settings
        </div>
    );
};

export default Profile;
